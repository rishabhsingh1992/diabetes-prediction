# Diabetes Prediction API

A minimal FastAPI project for serving diabetes prediction endpoints.

## Setup

1. Create and activate a virtual environment.
2. Install dependencies:

```bash
pip install -r requirements.txt
```

## Usage

Run the application:

```bash
python main.py
```

Then open your local API URL in the browser or use an API client to call prediction routes.
